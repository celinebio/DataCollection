## for input
import serial
from datetime import datetime
from serial.tools import list_ports
from collections import deque
from scipy.signal import savgol_filter  # Advanced smoothing

## for plotting
import matplotlib.pyplot as plt
import matplotlib.animation as ani

## Finds the port where Arduino is connected
def find_arduino_port():
    ports = list(list_ports.comports())
    for p in ports:
        if 'Arduino' in p.description:
            return p.device
    return None

## Apply Moving Average Filter
def moving_average(data, window_size=5):
    if len(data) < window_size:
        return sum(data) / len(data)  # Average of available values
    return sum(data[-window_size:]) / window_size  # Average of last `window_size` values

## Parses input from sensors and applies noise filtering
def parse_line(line):
    if "Accelerometer:" in line:
        try:
            parts = line.split("Accelerometer: ")[1].split(" ")
            accel_data = list(map(float, parts[1::2]))  # Convert to floats
            accel_data.insert(0, "A")  # Mark as accelerometer data
            
            # Apply moving average
            filtered_x = moving_average(accel_x_buffer, 5)
            filtered_y = moving_average(accel_y_buffer, 5)
            filtered_z = moving_average(accel_z_buffer, 5)

            # Store smoothed values
            accel_x_buffer.append(filtered_x)
            accel_y_buffer.append(filtered_y)
            accel_z_buffer.append(filtered_z)

            return ["A", filtered_x, filtered_y, filtered_z]
        except Exception as e:
            print(e)
            pass
    elif "Gyroscope:" in line:
        try:
            parts = line.split("Gyroscope: ")[1].split(" ")
            gyro_data = list(map(float, parts[1::2]))
            gyro_data.insert(0, "G")  # Mark as gyroscope data
            
            # Apply moving average
            filtered_x = moving_average(gyro_x_buffer, 5)
            filtered_y = moving_average(gyro_y_buffer, 5)
            filtered_z = moving_average(gyro_z_buffer, 5)

            # Store smoothed values
            gyro_x_buffer.append(filtered_x)
            gyro_y_buffer.append(filtered_y)
            gyro_z_buffer.append(filtered_z)

            return ["G", filtered_x, filtered_y, filtered_z]
        except Exception as e:
            print(e)
            pass
    else:
        try:
            emg_value = float(line)
            filtered_emg = moving_average(emg_buffer, 5)  # Apply moving average
            emg_buffer.append(filtered_emg)  # Store smoothed value
            return filtered_emg
        except Exception as e:
            print(e)
            pass

def animate(i, dataList, ser):
    try:
        if ser.in_waiting > 0:
            line = ser.readline().decode('utf-8').strip()
            dataPoint = parse_line(line)
            
            timestamp = datetime.now().strftime('%Y-%m-%d %H:%M:%S')  # Current timestamp
            print(f"{timestamp}, {line}")
            file.write(f"{timestamp},{line}\n")  # Save timestamp and data to file
            
            if type(dataPoint) == list:  # Adds data to appropriate lists
                if dataPoint[0] == "A":
                    dataList[4].append(dataPoint[1])
                    dataList[5].append(dataPoint[2])
                    dataList[6].append(dataPoint[3])
                else:
                    dataList[1].append(dataPoint[1])
                    dataList[2].append(dataPoint[2])
                    dataList[3].append(dataPoint[3])
            else:
                dataList[0].append(dataPoint)
    except KeyboardInterrupt:
        print(f"Data saved to {output_file}")
        ser.close()
    except:
        pass

    # Apply Savitzky-Golay Filter for extra smoothing
    for i in range(len(dataList)):
        if len(dataList[i]) > 10:
            dataList[i] = list(savgol_filter(dataList[i], 7, 3))  # 7-point window, 3rd-degree poly

    for i in range(len(dataList)):
        dataList[i] = dataList[i][-50:]  # Keep last 50 values

    # plots data
    ax1.clear()
    ax2.clear()
    ax3.clear()
    ax4.clear()
    ax5.clear()
    ax6.clear()
    ax7.clear()
    ax1.plot(dataList[0])
    ax2.plot(dataList[1])
    ax3.plot(dataList[2])
    ax4.plot(dataList[3])
    ax5.plot(dataList[4])
    ax6.plot(dataList[5])
    ax7.plot(dataList[6])

    # axis formatting
    ax1.set_title("EMG")
    ax1.set_ylim([0, 1000])
    ax2.set_title("Gyroscope")
    ax5.set_title("Accelerometer")
    ax5.set_ylabel("X")
    ax5.yaxis.set_label_position("right")
    ax6.set_ylabel("Y")
    ax6.yaxis.set_label_position("right")
    ax7.set_ylabel("Z")
    ax7.yaxis.set_label_position("right")
    plt.subplots_adjust(left=0.048, bottom=0.09, right=0.95, top=0.95, wspace=0.282, hspace=0.147)

# Connect to Arduino
arduino_port = find_arduino_port()
if arduino_port is None:
    print("Arduino not found. Check connection or modify find_arduino_port() function.")
    exit()

ser = serial.Serial(arduino_port, 9600)  # Connect to detected Arduino port

output_file = "data1_from_2diy.csv"
dataList = [[], [], [], [], [], [], []]  # Data for EMG, Gyroscope X, Y, Z, Accelerometer X, Y, Z

# Initialize buffers for moving average filter
buffer_size = 5
emg_buffer = deque(maxlen=buffer_size)
gyro_x_buffer = deque(maxlen=buffer_size)
gyro_y_buffer = deque(maxlen=buffer_size)
gyro_z_buffer = deque(maxlen=buffer_size)
accel_x_buffer = deque(maxlen=buffer_size)
accel_y_buffer = deque(maxlen=buffer_size)
accel_z_buffer = deque(maxlen=buffer_size)

file = open(output_file, 'w')
print("Logging data... Press Ctrl+C to stop.")
file.write("Timestamp,Data\n")

fig = plt.figure()
ax1 = fig.add_subplot(1, 3, 1)  # EMG
ax2 = fig.add_subplot(3, 3, 2)  # Gyro X
ax3 = fig.add_subplot(3, 3, 5)  # Gyro Y
ax4 = fig.add_subplot(3, 3, 8)  # Gyro Z
ax5 = fig.add_subplot(3, 3, 3)  # Accel X
ax6 = fig.add_subplot(3, 3, 6)  # Accel Y
ax7 = fig.add_subplot(3, 3, 9)  # Accel Z
print("Figure initialized")

ani = ani.FuncAnimation(fig, animate, frames=100, fargs=(dataList, ser), interval=200)
plt.show()

print(f"Data saved to {output_file}")
ser.close()
